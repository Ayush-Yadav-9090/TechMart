const AdminHeader = () => {
  return (
    <header className="admin-header">
      <h3>Admin Panel</h3>
      <button className="logout-btn">Logout</button>
    </header>
  );
};

export default AdminHeader;
